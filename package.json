{
  "name": "zero-to-dapp-demo",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ zero-to-dapp-demo",
    "create-local": "graph create --node http://localhost:8020/ zero-to-dapp-demo",
    "remove-local": "graph remove --node http://localhost:8020/ zero-to-dapp-demo",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zero-to-dapp-demo",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
